var bagStorage = JSON.parse(localStorage.bag);


window.BAG
id: document.getElementById("item").getAttribute("data-id"),
size: this.elements.size.value || size,
color: this.elements.color.value || color,
url: window.location.href,
name: name,
price: price